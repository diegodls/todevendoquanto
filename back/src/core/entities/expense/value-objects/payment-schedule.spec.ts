import { describe, expect, it } from "vitest";

describe("PaymentSchedule", () => {
  describe("create", () => {
    it("should do nothing, base test", () => {
      const expected = "value";
      const actual = "value";
      expect(actual).toBe(expected);
    });
  });
});
