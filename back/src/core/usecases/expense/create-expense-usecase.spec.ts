import { describe, expect, it } from "vitest";

describe("HOLDER", () => {
  it("should hold", () => {
    const expected = "value";
    const actual = "value";
    expect(actual).toBe(expected);
  });
});
